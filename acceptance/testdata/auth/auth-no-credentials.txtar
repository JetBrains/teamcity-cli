# When run without credentials, auth-requiring commands should fail gracefully.
# We unset all auth env vars to simulate a clean state.

env TEAMCITY_URL=
env TEAMCITY_TOKEN=
env TEAMCITY_GUEST=

! exec teamcity auth status --no-input
