# Filter jobs by project.

exec teamcity api /app/rest/projects?locator=count:1&fields=project(id) --raw --no-input
stdout '"id"'

# Listing jobs should work even with a filter that may not match
exec teamcity job list --limit 3 --no-input
! stderr 'Error'
